from django.urls import path, re_path
from apps.accounts import views

urlpatterns = [
    # path('user_list/', views.user_list),
    # path('user/<str:username>/', views.user_list),
    # path('user_profile/', views.profile),
]
